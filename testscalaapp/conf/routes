# Routes

POST    /numberOfPlaces     controllers.ManufactoryController.numberOfPlaces()
POST    /ship               controllers.ManufactoryController.ship()
GET     /next               controllers.ManufactoryController.next()


# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.versioned(path="/public", file: Asset)
